import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#0A3D62" },
    secondary: { main: "#1e272e" },
  },
  typography: {
    fontFamily: "Roboto, sans-serif",
  },
});
